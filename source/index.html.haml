---
title: DevOps Дефлопе
pageable: true
per_page: 1
---

- page_articles.each_with_index do |article, i|
  %article
    %h1
      = link_to article.title, article
      %time= Russian::strftime(article.date, '%B %e, %Y')
    = article.body
    .links.clearfix
      = link_to Russian::strftime(article.date, '%B %e, %Y'), article, class: 'go-more'

